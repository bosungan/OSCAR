.omc/
.RUD/

# rotation pipeline artifacts (dump tensors, computed rotation .pt, eval logs).
# Per-model: track only the .sh / .py source under rotation/<model>/; exclude
# every kind of generated calibration / eval / investigation byproduct.
rotation/*/GPQA/
rotation/*/LCB/
rotation/*/MATH/
rotation/*/MMLU/
rotation/*/Hadamard/
rotation/*/qkv_dumps*/
rotation/*/rotations*/
rotation/*/_eval_*/
rotation/*/investigation/

# MiniMax-M2.7 is internal-only for now (per-investigation work-in-progress).
rotation/MiniMax-M2.7/

# Python caches
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Editor / OS
.DS_Store
.idea/
.vscode/

# Build output (CMake / Gradle) -- e.g. /build, examples/llama.android/build
build/

# clangd / compiler index cache -- e.g. /.cache, build/.cache
.cache/

# Node / frontend deps & build artifacts (tools/ui)
node_modules/
.svelte-kit/
.vite-temp/

# Android / Gradle caches (examples/llama.android)
.gradle/
